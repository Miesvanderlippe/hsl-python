from timeit import timeit


def longest_in_column(subject: list, index: int)->str:
    longest_length = 0
    longest_length

    for item in subject:
        if len(item) > longest_length:
            longest_length = len(item)


def main()->None:
    return None

if __name__ == '__main__':
    main()
